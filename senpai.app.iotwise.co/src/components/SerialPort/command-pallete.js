export const cmd_execute = (cmd) => {
  switch (cmd) {

    case 'clear':
    case 'cls':
      break;

    case 'clear -r':
      break;

    case 'exit':
      break;

    case 'set interval':
      break;

    case 'clear interval':
      break;
      
    default:
      break;
  }

}